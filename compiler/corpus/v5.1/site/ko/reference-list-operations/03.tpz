let 숫자들 = Array.of(1, 2, 3, 4, 5)

let 짝수제곱합 = 숫자들
    |> filter(_, x => x % 2 == 0)
    |> map(_, x => x * x)
    |> reduce(_, 0, (누적, x) => 누적 + x)   // reduce(xs, initial, f)

print("짝수 제곱 합: {짝수제곱합}")  // 20
