let 온도 = 25
let 습도 = 60
let 계절 = "여름"

match { 온도: 온도, 습도: 습도, 계절: 계절 } {
    case { 온도, 습도, 계절: "여름" } if 온도 > 30 && 습도 > 70 => print("매우 덥고 습함")
    case { 온도, 습도, 계절: "여름" } if 온도 > 25 && 습도 < 50 => print("따뜻하고 건조함")
    case { 온도, 계절: "겨울" } if 온도 < 0 => print("매우 추움")
    case { 온도, 습도 } if 온도 >= 20 && 온도 <= 25 && 습도 >= 40 && 습도 <= 60 => print("쾌적한 날씨")
    case _ => print("보통 날씨")
}
