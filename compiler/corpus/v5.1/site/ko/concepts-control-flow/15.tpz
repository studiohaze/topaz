// 재귀적 패턴 매칭으로 리스트 처리
function 리스트합계(목록: Array<int>) -> int {
    match 목록 {
        case [] => 0
        case [첫번째, ..나머지] => 첫번째 + 리스트합계(나머지)
    }
}

print("리스트 합계: {리스트합계([1, 2, 3, 4, 5])}")  // 15
