function 배송라벨(상태: 주문상태) -> string {
    match 상태 {
        case "생성됨" => "결제 대기"
        case "결제완료" => "배송 준비 중"
        case "배송중" => "이동 중"
        case "배송완료" => "배송됨"
        case "취소됨" => "취소됨"
    }
}
