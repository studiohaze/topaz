// |> 가 가장 느슨하게 결합하므로 산술이 먼저 일어납니다
let 두배 = (x: int) => x * 2
let 파이프결과 = 3 + 2 |> 두배()      // 10 — (3 + 2) |> 두배() 와 동일

// 범위 우선순위: 덧셈류가 .. 보다 먼저 결합
let 범위 = (1 + 0)..(5 - 0)
let 포함됨 = 3 in 범위                // true
