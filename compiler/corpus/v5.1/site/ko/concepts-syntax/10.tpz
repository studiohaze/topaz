let 이름들 = ["안", "보", "초"]

let 요약 = 이름들
  |> .length                  // 속성 슈거: (xs => xs.length)
  |> format("개수=", _)       // 플레이스홀더: (n => format("개수=", n))

// 가장 가까운 호출 안의 모든 _ 는 같은 파이프 값을 받습니다
let 한줄 = 이름들 |> format("첫번째 ", _, " 전체 ", _)
