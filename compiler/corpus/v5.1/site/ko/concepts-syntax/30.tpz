let 분석결과 = CSV.읽기("sales.csv")
    |> 필터(행 => 행.매출 > 1000000)
    |> 그룹화(행 => 행.지역)
    |> 집계(그룹 => {
        지역: 그룹.키,
        총매출: 그룹.값.합계(행 => 행.매출),
        평균매출: 그룹.값.평균(행 => 행.매출)
    })
    |> 정렬(내림차순: 행 => 행.총매출)
