// 레코드 리터럴
let 사용자 = {
    이름: "김토파즈",
    나이: 25,
    이메일: "topaz@example.com"
}

// 레코드 타입 별칭
type 사용자정보 = {
    이름: string,
    나이: int,
    이메일: string,
}

// 별칭 타입 명시가 붙은 레코드 값
let 새사용자: 사용자정보 = {
    이름: "박루비",
    나이: 30,
    이메일: "ruby@example.com",
}
