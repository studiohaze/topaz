// 복잡한 타입에 간단한 이름 부여
type 사용자정보 = {
    이름: string,
    나이: int,
    이메일: string,
    권한들: Array<string>
}

type 콜백함수 = (string) -> ()

// 사용 예시
let 관리자: 사용자정보 = {
    이름: "김관리자",
    나이: 35,
    이메일: "admin@topaz.ooo",
    권한들: ["읽기", "쓰기", "삭제"]
}
