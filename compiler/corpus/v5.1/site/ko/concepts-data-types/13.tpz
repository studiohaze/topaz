// 배열 구조 분해
let [첫번째, 두번째, ..나머지] = [1, 2, 3, 4, 5]
print("{첫번째}")    // 1
print("{나머지}")    // [3, 4, 5]

// 객체 구조 분해
let { 이름, 나이 } = 사용자
print("이름: {이름}, 나이: {나이}")

// 함수 본문 안에서 구조 분해
function 인사하기(사용자: { 이름: string, 나이: int }) {
    let { 이름, 나이 } = 사용자
    print("안녕하세요, {이름}님! {나이}세이시군요.")
}
