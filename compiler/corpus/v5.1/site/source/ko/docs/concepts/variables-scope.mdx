---
title: '변수와 스코프'
description: '토파즈에서 변수 선언, 스코프 관리, 가변성 제어를 마스터하세요. let, let mut, const의 차이점과 렉시컬 스코핑, 클로저의 동작 원리를 배워보세요.'
covers: '§3, §4, §5, §6, §18'
---

> **라이브러리 이름 안내**: 표준 라이브러리 최소 표면을 벗어나는 도우미 이름(`사용자목록` 등)은 설명용 placeholder이며 canonical API가 아닙니다.

변수와 스코프는 프로그래밍의 핵심입니다. 토파즈는 **현대적이고 안전한 변수 시스템**을 제공하여 버그를 줄이고 코드의 명확성을 높입니다. 

##  변수 선언

### let - 불변 변수 (기본)

```topaz
// 기본적으로 모든 변수는 불변입니다
let 이름 = "김토파즈"
let 나이 = 25
let 활성화됨 = true

// 한 번 정의된 후에는 변경할 수 없습니다
// 이름 = "다른이름"  // 컴파일 에러!
```

Topaz 공개 문서에서는 변수를 재할당하거나 컬렉션을 제자리 변경할 때 `let mut`를 사용합니다. 안정적인 바인딩에는 기본 `let`를 사용합니다.

### let mut - 가변 변수

```topaz
// 값을 변경해야 할 때는 let mut 사용
let mut 점수 = 100
let mut 메시지 = "초기 메시지"

// 값 변경 가능
점수 = 95
메시지 = "업데이트된 메시지"

print("현재 점수: {점수}")  // "현재 점수: 95"
```

### const - 컴파일 타임 상수

```topaz
// 컴파일 시점에 결정되는 상수
const MAX_SIZE: int = 1000
const APP_NAME: string = "Topaz Calculator"
const PI: float = 3.141592653589793

// 같은 파일 아래에서 사용 가능한 최상위 상수
const 기본설정 = {
    언어: "한국어",
    테마: "다크",
    자동저장: true
}
```

##  타입 추론과 명시적 타입

```topaz
// 타입 추론 (권장)
let 자동추론숫자 = 42           // int
let 자동추론문자열 = "Hello"     // string
let 자동추론배열 = [1, 2, 3]    // Array<int>

// 명시적 타입 (필요할 때)
let 명시적숫자: int = 42
let 명시적문자열: string = "Hello"
let 명시적배열: Array<int> = [1, 2, 3]

// 복잡한 경우 타입 명시가 도움됨
let 사용자데이터: { 이름: string, 나이: int } = {
    이름: "김개발자",
    나이: 30
}
```

##  스코프 규칙

### 전역 스코프

```topaz
// 파일 최상위 레벨 - 전역 스코프
let 전역변수 = "모든 곳에서 접근 가능"
const 전역상수 = 100

function 어디서든사용() {
    print(전역변수)      // 접근 가능
    print("{전역상수}")  // 접근 가능
}
```

### 함수 스코프

```topaz
function 계산기(a: int, b: int) -> int {
    // 함수 매개변수도 함수 스코프에 속함
    let 결과 = a + b        // 함수 스코프
    let mut 임시값 = a * 2      // 함수 스코프
    
    print("결과: {결과}")
    return 결과
}

// print(결과)  // 에러! 함수 밖에서 접근 불가
```

### 블록 스코프

```topaz
function 스코프예제() {
    let 외부변수 = "바깥"
    
    if true {
        let 내부변수 = "안쪽"        // 블록 스코프
        let mut 블록가변 = 10            // 블록 스코프
        
        print(외부변수)              // 접근 가능 (상위 스코프)
        print(내부변수)              // 접근 가능 (같은 스코프)
    }
    
    print(외부변수)                  // 접근 가능
    // print(내부변수)               // 에러! 블록 밖에서 접근 불가
}
```

### 루프 스코프

```topaz
// for 루프의 변수는 루프 스코프를 가짐
for i in 0..5 {
    let 루프내변수 = i * 2
    print("i: {i}, 값: {루프내변수}")
}

// print(i)  // 에러! 루프 밖에서 접근 불가

```

> Topaz는 `for`와 함께 `while` 문도 제공합니다 (제어 흐름 페이지 참고). `while` 본문에도 같은 블록 스코프 규칙이 적용됩니다.

##  섀도잉 (Variable Shadowing)

```topaz
let 이름 = "김토파즈"

function 섀도잉예제() {
    let 이름 = "박개발자"        // 전역 이름을 가림 (섀도잉)
    print("함수 내: {이름}")     // "함수 내: 박개발자"
    
    {
        let 이름 = "이디자이너"   // 함수의 이름을 가림
        print("블록 내: {이름}")  // "블록 내: 이디자이너"
    }
    
    print("블록 후: {이름}")     // "블록 후: 박개발자"
}

print("전역: {이름}")           // "전역: 김토파즈"
섀도잉예제()
```

> 위 예시처럼 **중첩된** 어휘 스코프에서의 섀도잉은 canonical입니다. **같은** 스코프에서 같은 이름을 재선언하는 것은 canonical Topaz가 아닙니다.

##  클로저와 스코프

```topaz
function 카운터생성기(초기값: int) -> () -> int {
    let mut 개수 = 초기값           // 클로저에 캡처됨
    
    return () => {
        개수 += 1               // 상위 스코프의 변수 접근
        개수
    }
}

let 카운터1 = 카운터생성기(0)
let 카운터2 = 카운터생성기(100)

print("{카운터1()}")  // 1
print("{카운터1()}")  // 2
print("{카운터2()}")  // 101
print("{카운터1()}")  // 3
```

##  패턴 매칭과 변수

```topaz
let 사용자 = {
    이름: "김토파즈",
    나이: 28,
    직업: "개발자"
}

// 구조 분해로 새 변수 생성
let { 이름, 나이 } = 사용자
print("이름: {이름}, 나이: {나이}")

// match에서 변수 바인딩
match 사용자.나이 {
    case 나이 if 나이 < 20 => print("학생일 가능성이 높음")
    case 나이 if 나이 < 30 => print("청년층")
    case 나이 => print("경력자: {나이}세")
}

// 배열 패턴 매칭
let 점수들 = [95, 87, 92, 88]
match 점수들 {
    case [첫번째, 두번째, ..나머지] => {
        print("첫 번째: {첫번째}")
        print("두 번째: {두번째}")
        print("나머지: {나머지}")
    }
    case _ => print("점수가 두 개 이상 필요합니다")
}
```

##  고급 스코프 패턴

### 블록 표현식으로 스코프 격리

```topaz
// 블록 표현식으로 스코프 격리
let 결과 = {
    let 임시계산 = 10 * 20
    let 보정값 = 50
    임시계산 + 보정값
}

print("{결과}")  // 250
// 임시계산, 보정값은 블록 밖에서 접근 불가
```

### 모듈 스코프

> Topaz v5.2는 모듈 / `import` / `export` 시스템을 추가합니다. 다중 파일 구성, 가시성 규칙, 모듈 스코프 바인딩은 [모듈과 가시성](/ko/docs/concepts/modules)에서 다루며, 이 페이지의 예시는 단일 파일 안에 머뭅니다.

##  베스트 프랙티스

### 1. 기본적으로 불변 사용

```topaz
// 좋은 예: 기본적으로 let 사용
let 설정 = {
    테마: "다크",
    언어: "한국어"
}

// 필요할 때만 let mut 사용
let mut 진행상태 = "시작"
진행상태 = "진행중"
진행상태 = "완료"
```

### 2. 의미있는 변수명

```topaz
// 나쁜 예
let a = 사용자목록.length
let b = a * 2

// 좋은 예  
let 총사용자수 = 사용자목록.length
let 두배수용량 = 총사용자수 * 2
```

### 3. 스코프 최소화

```topaz
function 데이터처리(원본데이터: Array<int>) -> Array<int> {
    let mut 결과: Array<int> = []
    
    for 항목 in 원본데이터 {
        // 루프 내에서만 필요한 변수는 루프 내에서 선언
        let 변환된값 = 항목 * 2 + 1
        let 검증된값 = if 변환된값 > 0 { 변환된값 } else { 0 }
        
        결과.push(검증된값)
    }
    
    return 결과
}
```

##  디버깅 팁

```topaz
// 보간 print로 바인딩 추적
let 전역라벨 = "build-42"

function 스코프정보출력() {
    let 지역변수 = "local"
    
    print("현재 함수의 변수들:")
    print("- 지역변수: {지역변수}")
    print("- 전역라벨: {전역라벨}")   // 바깥 바인딩은 계속 접근 가능
}
```

이름 해석은 정적입니다: 스코프에 없는 이름을 사용하면 컴파일 타임 에러이므로, 런타임에 "이 변수가 정의되어 있나?"를 검사하는 코드를 작성할 일이 없습니다 — 컴파일러가 프로그램 실행 전에 잡아냅니다.

토파즈의 변수와 스코프 시스템은 **안전성**과 **명확성**을 동시에 제공합니다. 불변성을 기본으로 하되 필요할 때 가변성을 허용하고, 명확한 스코프 규칙으로 예측 가능한 코드를 작성할 수 있습니다. 
