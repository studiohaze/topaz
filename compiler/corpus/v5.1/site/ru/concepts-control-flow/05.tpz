// Накапливаем, пока не достигнем бюджета
let mut сумма = 0
let mut n = 1

while сумма + n <= 100 {
    сумма = сумма + n
    n = n + 1
}

print("Сумма в пределах бюджета: {сумма}")

// Обратный отсчёт
let mut отсчёт = 3
while отсчёт > 0 {
    print("Т-минус {отсчёт}")
    отсчёт = отсчёт - 1
}
