// Рекурсивное сопоставление образцов для обработки списка
function суммаСписка(список: Array<int>) -> int {
    match список {
        case [] => 0
        case [первый, ..остальные] => первый + суммаСписка(остальные)
    }
}

print("Сумма списка: {суммаСписка([1, 2, 3, 4, 5])}")  // 15
