function ярлыкДоставки(статус: СтатусЗаказа) -> string {
    match статус {
        case "создан" => "Ожидает оплаты"
        case "оплата_завершена" => "Готовится к отправке"
        case "доставка" => "В пути"
        case "доставлен" => "Доставлен"
        case "отменен" => "Отменён"
    }
}
