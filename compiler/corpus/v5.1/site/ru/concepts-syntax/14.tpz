// Итерация по диапазону
for i in 1..10 {
    print("Число: {i}")
}

// Итерация по массиву
for элемент in ["яблоко", "банан", "вишня"] {
    print("Фрукт: {элемент}")
}

// for тоже возвращает значения!
let квадраты = for x in 1..5 { x * x }  // [1, 4, 9, 16, 25]
