let mut сумма = 0
let mut n = 1

while сумма + n <= 100 {
    сумма = сумма + n
    n = n + 1
}

print("сумма: {сумма}")

// break / continue нацелены на самый внутренний цикл
for элемент in [3, -1, 7, 120, 5] {
    if элемент < 0 { continue }
    if элемент > 99 { break }
    print("{элемент}")
}
