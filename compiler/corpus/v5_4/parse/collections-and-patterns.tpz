let xs = [ for x in [1, 2, 3, 4] if x > 2 => x * 2 ]
let s = set { for x in xs => x }
let m = map { for x in xs => "{x}": x }
let first = if let Some(n) = xs.get(0) { n } else { -1 }
print("{first} {s.length} {m.length}")
