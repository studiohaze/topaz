
function spin(x: int) -> int {
    x
}

let mut callbacks = { primary: spin, count: 0 }
callbacks.count += 1

concurrent {
    slow: {
        let xs = [1]
        xs.map(callbacks.primary)
    }
    fast: 0
}
0
