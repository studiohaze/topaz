
function spin(x: int) -> int {
    x
}

let base = [spin]
let callbacks = [...base, spin]
let seeds = [0, 1]
let i = seeds[1]

concurrent {
    slow: {
        let xs = [1]
        xs.map(callbacks[i])
    }
    fast: 0
}
0
