
function spin(x: int) -> int {
    x
}

let mut callbacks = { primary: spin }
callbacks.primary = (x) => x

concurrent {
    slow: {
        let xs = [1]
        xs.map(callbacks.primary)
    }
    fast: 0
}
0
