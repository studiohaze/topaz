
function boom() -> int {
    let n = 0
    1 / n
}

function six() -> int {
    6
}

function choose(x: int) -> int {
    let i = x - 2
    concurrent {
        slow: {
            let mut cbs = [six, six]
            cbs[i] = boom
            let values: Array<Option<int>> = [None, Some(9)]
            let out = values[1].okOrElse(cbs[i])
            match out {
                case Ok(v) => v
                case Err(code) => code
            }
        }
        fast: 0
    }
    0
}

choose(3)
