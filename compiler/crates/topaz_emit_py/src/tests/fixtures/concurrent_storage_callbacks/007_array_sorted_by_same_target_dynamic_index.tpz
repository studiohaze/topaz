
function idKey(x: int) -> int {
    x
}

function negKey(x: int) -> int {
    0 - x
}

let seeds = [0, 1]
let i = seeds[1]
let callbacks = [idKey, negKey]

concurrent {
    slow: {
        let xs = [3, 1, 2]
        xs.sortedBy(callbacks[i])
    }
    fast: 0
}
0
