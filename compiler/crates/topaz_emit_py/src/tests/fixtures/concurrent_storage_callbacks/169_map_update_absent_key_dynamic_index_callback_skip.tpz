
function spin(x: int) -> int {
    x / 0
}

let seeds = [0, 1]
let i = seeds[1]
let callbacks = [spin, spin]

concurrent {
    slow: {
        let mut values = map { "a": 1 }
        values.update("z", 7, callbacks[i])
    }
    fast: 0
}
0
