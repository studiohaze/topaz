//! Data and rendering support for the Python differential build script.
//! Fixture selection remains in `fixture_index`, while this boundary shapes the
//! checked Rust reference emitted into `OUT_DIR`.

pub(super) mod model;
pub(super) mod render;
