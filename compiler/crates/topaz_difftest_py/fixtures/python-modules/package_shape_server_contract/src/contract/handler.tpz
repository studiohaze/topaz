
import contract.request { Request }
import contract.response { Response, text }

export function handle(req: Request) -> Response {
    if req.path == "/health" {
        text(200, "ok")
    } else {
        text(404, req.path)
    }
}
