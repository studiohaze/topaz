
import util
let mut first = util.spin
let mut second = first
let mut reserved = util.sortedBy
let mut mutableReserved = reserved
concurrent {
    valueChain: {
        let xs = [1]
        xs.map(second)
    }
    reservedChain: {
        let ys = [1]
        ys.map(mutableReserved)
    }
    boom: {
        let zs = [1]
        zs[2]
    }
}
0
