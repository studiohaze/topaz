
import util
function main() -> int {
    let first = util.plus
    let mut second = first
    let reserved = util.add
    let mut mutableReserved = reserved
    second(a: 5) + second(10, b: 3) + mutableReserved(a: 7) + mutableReserved(8, b: 4)
}
main()
