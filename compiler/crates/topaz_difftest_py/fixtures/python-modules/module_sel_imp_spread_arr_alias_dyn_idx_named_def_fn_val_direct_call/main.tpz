
import util { base }

function sub(a: int, b: int = 10) -> int {
    a - b
}

function main() -> int {
    let spread = [...base, sub]
    let callbacks = spread
    let i = 0
    let j = 1
    callbacks[i](a: 5) * 10 + callbacks[j](a: 15)
}
main()
