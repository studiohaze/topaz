
function main() -> int {
    let mut seed = 1
    function bad() -> int {
        if true {
            let mut seed = 0
            seed = seed + 1
        }
        seed = seed + 1
        seed
    }
    let got = bad()
    seed + got
}
main()
