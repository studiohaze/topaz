
function makeMap() {
    (map { "x": 4, "fallback": 9 })
}
function main() -> int {
    let values = makeMap()
    let bonus = if values.containsKey(k: "x") { 10 } else { 0 }
    bonus + values.getOr(k: "x", default: 0) + values.getOr(k: "missing", default: 7)
}
main()
