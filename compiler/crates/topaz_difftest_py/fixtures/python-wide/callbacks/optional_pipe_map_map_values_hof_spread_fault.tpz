
function mark(label: string, xs: Array<int>) -> Array<int> {
    print(label)
    xs
}
function inc(x: int) -> int { x + 1 }
function main() -> string {
    let m: Option<Map<string, int>> = Some(map { "x": 1 })
    inc |> m?.mapValues(...mark("pipe-map-values", [0]))
    "unreachable"
}
main()
