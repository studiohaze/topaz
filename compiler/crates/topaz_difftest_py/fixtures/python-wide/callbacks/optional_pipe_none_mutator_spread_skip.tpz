
function mark(label: string, xs: Array<int>) -> Array<int> {
    print(label)
    xs
}
function key(x: int) -> int { x }
function main() -> string {
    let mut xs: Option<Array<int>> = None
    key |> xs?.sortBy(...mark("pipe-none-sort", [0]))
    "done"
}
main()
