
function okInc(x: int) -> Result<int, string> {
    Ok(x + 1)
}
function main() -> int {
    let res = Err("bad").flatMap(okInc)
    match res {
        case Ok(_) => 0
        case Err(_) => 1
    }
}
main()
