
function mark(label: string, xs: Array<int>) -> Array<int> {
    print(label)
    xs
}
function key(x: int) -> int { x }
function main() -> string {
    let mut xs: Option<Array<int>> = Some([2, 1])
    key |> xs?.sortBy(...mark("pipe-sort", [0]))
    "unreachable"
}
main()
