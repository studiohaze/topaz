
function mark(label: string, xs: Array<int>) -> Array<int> {
    print(label)
    xs
}
function keep(x: int) -> bool { x > 0 }
function main() -> string {
    let mut xs: Option<Array<int>> = Some([1])
    keep |> xs?.retain(...mark("pipe-retain", [0]))
    "unreachable"
}
main()
