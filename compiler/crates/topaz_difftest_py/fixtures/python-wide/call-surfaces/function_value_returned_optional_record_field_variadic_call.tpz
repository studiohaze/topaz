
function sum(seed: int = 0, ...xs: int) -> int {
    let mut total = seed
    for x in xs {
        total = total + x
    }
    total
}
function maybeCallbacks() {
    Some({ total: sum })
}
function main() -> int {
    maybeCallbacks()?.total(3, ...[1, 2]) ?? 0
}
main()
