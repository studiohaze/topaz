
function mark(label: string, xs: Array<int>) -> Array<int> {
    print(label)
    xs
}
function main() -> Map<string, int> {
    let mut m = map { "x": 1 }
    m.clear(...mark("map-clear", []))
    m
}
main()
