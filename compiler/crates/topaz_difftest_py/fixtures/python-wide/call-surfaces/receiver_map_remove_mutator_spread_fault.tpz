
function mark(label: string, xs: Array<string>) -> Array<string> {
    print(label)
    xs
}
function main() -> Map<string, int> {
    let mut m = map { "x": 1 }
    m.remove(...mark("map-remove", ["x"]))
    m
}
main()
