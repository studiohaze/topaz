
function keep(x: int) -> bool {
    x > 2
}

function moreThanOne(x: int) -> bool {
    x > 1
}

function all(x: int) -> bool {
    x > 0
}

function choose(i: int) -> int {
    let r = concurrent {
        slow: {
            let mut cbs = [all, moreThanOne]
            match i {
                case 0 => -1
                case 1 => {
                    cbs[i] = keep
                    let xs = [1, 2, 3, 4]
                    let out = xs.filter(cbs[i])
                    out.length * 100 + out[0]
                }
                case _ => -2
            }
        }
        fast: 0
    }
    r.slow + r.fast
}

choose(1)
