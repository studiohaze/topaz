
function mul(acc: int, x: int) -> int {
    acc * x
}

function sub(acc: int, x: int) -> int {
    acc - x
}

function add(acc: int, x: int) -> int {
    acc + x
}

function choose(i: int) -> int {
    let r = concurrent {
        slow: {
            let mut cbs = [add, sub]
            let mut out = -9
            let mut n = 0
            while n < 1 {
                cbs[i] = mul
                let xs = [1, 2, 3]
                out = xs.reduce(10, cbs[i])
                n = n + 1
            }
            out
        }
        fast: 0
    }
    r.slow + r.fast
}

choose(1)
