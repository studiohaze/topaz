
function spin(x: int) -> int {
    let mut i = 0
    while i < 200 {
        i = i + 1
    }
    1 / 0
}
function same(x: int) -> int {
    x
}

let pipeline = spin >> same

concurrent {
    slow: {
        let xs = [1]
        xs.map(pipeline)
    }
    boom: {
        let ys = [1]
        ys[2]
    }
}
0
