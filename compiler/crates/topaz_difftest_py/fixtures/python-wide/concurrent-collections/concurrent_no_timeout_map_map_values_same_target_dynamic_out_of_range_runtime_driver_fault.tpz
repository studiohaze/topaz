
function spin(x: int) -> int {
    x * 2
}

let seeds = [2]
let i = seeds[0]
let callbacks = [spin, spin]

concurrent {
    slow: {
        let values = map { "a": 1, "b": 2 }
        values.mapValues(callbacks[i])
    }
    fast: 0
}
0
