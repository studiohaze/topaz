
function fail() -> string {
    let mut i = 0
    while i < 200 {
        i = i + 1
    }
    "{1 / 0}"
}

concurrent {
    slow: {
        let value: Option<int> = None
        value.okOrElse(fail)
    }
}
0
