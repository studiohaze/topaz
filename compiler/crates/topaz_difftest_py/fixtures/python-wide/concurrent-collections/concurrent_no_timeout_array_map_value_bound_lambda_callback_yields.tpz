
function spin(x: int) -> int {
    let mut i = 0
    while i < 200 {
        i = i + 1
    }
    1 / 0
}

let cb = (x) => spin(x)

concurrent {
    slow: {
        let xs = [1]
        xs.map(cb)
    }
    boom: {
        let ys = [1]
        ys[2]
    }
}
0
