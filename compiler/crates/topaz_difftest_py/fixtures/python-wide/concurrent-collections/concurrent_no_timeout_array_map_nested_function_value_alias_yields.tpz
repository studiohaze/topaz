
function main() -> int {
    concurrent {
        slow: {
            function spin(x: int) -> int {
                let mut i = 0
                while i < 200 {
                    i = i + 1
                }
                1 / 0
            }
            let alias = spin
            let xs = [1]
            xs.map(alias)
        }
        boom: {
            let ys = [1]
            ys[2]
        }
    }
    0
}
main()
