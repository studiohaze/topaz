
function spin(x: int) -> int {
    let mut i = 0
    while i < 200 {
        i = i + 1
    }
    1 / 0
}

concurrent {
    slow: {
        let value = Some(1)
        value.map((x) => spin(x))
    }
}
0
