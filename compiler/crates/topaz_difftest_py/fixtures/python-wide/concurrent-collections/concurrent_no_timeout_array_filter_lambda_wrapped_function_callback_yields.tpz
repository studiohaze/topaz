
function keep(x: int) -> bool {
    let mut i = 0
    while i < 200 {
        i = i + 1
    }
    (1 / 0) == 0
}

concurrent {
    slow: {
        let xs = [1]
        xs.filter((x) => keep(x))
    }
}
0
