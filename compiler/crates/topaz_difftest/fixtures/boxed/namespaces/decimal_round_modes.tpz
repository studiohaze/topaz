match Decimal.parse("2.5") { case Some(a) => match Decimal.parse("-2.5") { case Some(b) => "{a.round(0).toString()}/{a.round(0, RoundingMode.HalfUp).toString()}/{b.round(0, RoundingMode.HalfEven).toString()}/{b.round(0, RoundingMode.Down).toString()}"
case None => "none" }
case None => "none" }